# REQUIRES-PROPERTIES-FILE - # Do not remove this comment!
CREATE OR REPLACE DATASOURCE DF ds_local_user
    ROUTE LOCAL 'LocalConnection' '${databases.denodo_training.datasources.df.ds_local_user.ROUTE.LOCAL.PATH}'
    COLUMNDELIMITER = ';'
    ENDOFLINEDELIMITER = '\n'
    HEADER = TRUE;

