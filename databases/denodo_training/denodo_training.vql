CREATE OR REPLACE DATABASE denodo_training '';

ALTER DATABASE denodo_training
  CHARSET DEFAULT;

ALTER DATABASE denodo_training
  CACHE ON (
    MAINTENANCE OFF
    MAINTAINERPERIOD 21600
    TIMETOLIVE 3600
    DATASOURCE DEFAULT
  );

